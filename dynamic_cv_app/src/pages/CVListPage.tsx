import React from "react";

const CVListPage = () => {
  return (
    <div>
      <h2>CV List</h2>
      {/* Se liste over CV er (bare for admin eller for alle?) */}
    </div>
  );
};

export default CVListPage;
